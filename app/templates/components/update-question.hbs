{{#if updateQuestionForm}}
  <form>
    <div class="form-group">
      {{input value=query id="updatequery" placeholder="Update Question"}}
    </div>

    <div class="form-group">
      {{input value=note id="updatenote" placeholder="Update Info"}}
    </div>

    <button class="btn btn-info"{{action 'update' question}}>Submit</button>
  </form>
{{else}}
  <button class="btn btn-info"{{action 'updateQuestionForm'}}>Update</button>
{{/if}}
